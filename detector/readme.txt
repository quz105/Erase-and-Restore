This folder contains:
1) The script (in Jupyter NoteBook) used to train the AE detector (based on AdaBoost and SVM)
2) Two well-trained AE detectors (one is an AdaBoost classifier, and the other one is a SVM classifier)
